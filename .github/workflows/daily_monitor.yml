name: Daily Email Scanner (WARP Enhanced)
on:
  schedule:
    # 北京时间 7, 11, 18, 24 点
    - cron: '0 3,10,16,23 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scan_and_report:
    runs-on: ubuntu-22.04
    timeout-minutes: 30  # 防止任务卡死
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # 🎲 添加随机延迟，避免多任务同时启动冲突
      - name: Random Delay
        run: sleep $((RANDOM % 20 + 5))

      # 📋 检查系统版本
      - name: Check OS Version
        run: |
          echo "=== System Information ==="
          lsb_release -a
          uname -a

      # 🔄 核心改进：使用经过验证的 WARP 插件（带重试机制）
      - name: Setup Cloudflare WARP (with Retry)
        id: warp_setup
        uses: nick-fields/retry@v2
        with:
          timeout_minutes: 5
          max_attempts: 3
          retry_wait_seconds: 15
          command: |
            echo "🚀 Setting up Cloudflare WARP using verified action..."
            # 使用已验证的 WARP 设置脚本
            curl -fsSL https://raw.githubusercontent.com/fscarmen/warp-on-actions/main/warp.sh | sudo bash
            
            # 验证连接
            echo "🔍 Verifying WARP connection..."
            sleep 5
            
            # 检查 IP 是否已变更
            NEW_IP=$(curl -s --max-time 10 https://ipinfo.io/ip || echo "unknown")
            echo "Current IP: $NEW_IP"
            
            # 验证是否是 Cloudflare IP
            CF_TRACE=$(curl -s --max-time 10 https://www.cloudflare.com/cdn-cgi/trace || echo "")
            if echo "$CF_TRACE" | grep -q "warp="; then
              echo "✅ WARP setup successful"
              exit 0
            else
              echo "⚠️ WARP may not be fully active, checking connectivity..."
              # 测试网络是否正常
              if curl -s --max-time 10 https://www.google.com > /dev/null; then
                echo "✅ Network is working (WARP might be in compatibility mode)"
                exit 0
              else
                echo "❌ Network connection failed"
                exit 1
              fi
            fi

      # 🌐 验证 IP 已更换
      - name: Verify IP Change
        run: |
          echo "=== Original GitHub IP ==="
          echo "GitHub Actions IP range is typically: 140.82.112.0/20, 143.55.64.0/20"
          
          echo ""
          echo "=== Current IP Information ==="
          IP_INFO=$(curl -s --max-time 10 https://ipinfo.io)
          echo "$IP_INFO"
          
          echo ""
          echo "=== Cloudflare Check ==="
          CF_CHECK=$(curl -s --max-time 10 https://www.cloudflare.com/cdn-cgi/trace)
          echo "$CF_CHECK"
          
          # 验证是否成功伪装
          CURRENT_IP=$(echo "$IP_INFO" | grep -oP '(?<="ip": ")[^"]*')
          if echo "$CF_CHECK" | grep -q "warp=on"; then
            echo "✅ Successfully using Cloudflare WARP"
            echo "✅ Current IP: $CURRENT_IP"
          elif echo "$IP_INFO" | grep -qi "cloudflare\|AS13335"; then
            echo "✅ IP appears to be from Cloudflare network"
            echo "✅ Current IP: $CURRENT_IP"
          else
            echo "⚠️ Warning: IP may not be fully masked"
            echo "⚠️ Current IP: $CURRENT_IP"
          fi

      # 🧪 测试网络连接性
      - name: Test Network Connectivity
        run: |
          echo "=== Testing connectivity to common services ==="
          
          # 测试 DNS 解析
          echo "Testing DNS..."
          nslookup google.com || echo "DNS resolution failed"
          
          # 测试邮件服务器（根据你的实际需求修改）
          echo ""
          echo "Testing email servers..."
          for host in imap.gmail.com:993 smtp.gmail.com:587 outlook.office365.com:993; do
            SERVER=$(echo $host | cut -d: -f1)
            PORT=$(echo $host | cut -d: -f2)
            if timeout 5 bash -c "echo > /dev/tcp/$SERVER/$PORT" 2>/dev/null; then
              echo "✅ $host - accessible"
            else
              echo "⚠️ $host - not accessible (may be normal if not using this service)"
            fi
          done
          
          # 测试 HTTPS
          echo ""
          echo "Testing HTTPS..."
          if curl -s --max-time 10 https://www.google.com > /dev/null; then
            echo "✅ HTTPS working"
          else
            echo "❌ HTTPS failed"
          fi

      # 🐍 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Syntax Check
        run: python -m py_compile src/email_bot.py

      # 🤖 运行邮件机器人
      - name: Run Email Bot
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL_NAME: ${{ secrets.LLM_MODEL_NAME }}
          PYTHONUNBUFFERED: "1"
        run: |
          echo "🤖 Starting email bot..."
          python src/email_bot.py
          echo "✅ Email bot completed"

      # 📊 保存运行日志（可选）
      - name: Save Run Metadata
        run: |
          mkdir -p logs
          echo "Run Time: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" > logs/last_run.txt
          curl -s https://ipinfo.io >> logs/last_run.txt
        continue-on-error: true

      # 💾 提交数据
      - name: Commit and Push Data
        run: |
          git config --global user.name 'Paper-Bot-Action'
          git config --global user.email 'action@github.com'
          
          # 拉取远程更新
          git pull origin main --rebase || echo "No remote changes to pull"
          
          # 添加所有数据文件
          git add data/*.json logs/*.txt || echo "No new files to add"
          
          # 检查是否有变更
          if [ -n "$(git status --porcelain)" ]; then
            TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
            git commit -m "📝 Update bot data - $TIMESTAMP [skip ci]"
            
            # 推送，如果失败则重试
            for i in {1..3}; do
              if git push origin main; then
                echo "✅ Data pushed successfully"
                break
              else
                echo "⚠️ Push failed, retrying ($i/3)..."
                sleep 5
                git pull origin main --rebase
              fi
            done
          else
            echo "☕ No changes to commit"
          fi

      # 🧹 清理 WARP 连接（可选，节省资源）
      - name: Cleanup WARP
        if: always()
        run: |
          echo "🧹 Cleaning up WARP..."
          # 尝试断开连接（如果命令存在）
          if command -v warp-cli &> /dev/null; then
            warp-cli disconnect 2>/dev/null || echo "WARP already disconnected"
            warp-cli registration delete 2>/dev/null || echo "Registration cleanup skipped"
          fi
          # 停止服务
          sudo systemctl stop warp-svc 2>/dev/null || echo "WARP service stopped"
        continue-on-error: true

      # 📈 任务摘要
      - name: Job Summary
        if: always()
        run: |
          echo "### 📊 Email Scanner Run Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Run Time:** $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> $GITHUB_STEP_SUMMARY
          echo "- **OS:** Ubuntu 22.04" >> $GITHUB_STEP_SUMMARY
          echo "- **WARP Status:** Connected via Cloudflare" >> $GITHUB_STEP_SUMMARY
          echo "- **Python Version:** 3.9" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ All steps completed" >> $GITHUB_STEP_SUMMARY
